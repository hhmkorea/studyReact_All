import { Article, getTriangle } from "./App.js"; // import : 모듈 가져오기 

console.log(getTriangle(10,5)); // App.js의 함수 호출.

const a = new Article();
console.log(a.getAppTitle()); // App.js의 Article 클래스 안의 함수 호출.